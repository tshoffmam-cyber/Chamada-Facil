# Esqueleto de referencia (ver backend/README.md) -- versoes indicativas,
# nao testadas em producao.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
# Quando o banco real entrar (PostgreSQL, conforme o PRD):
# sqlmodel==0.0.22
# psycopg2-binary==2.9.9
# alembic==1.13.2
# Quando a autenticacao real entrar:
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4
