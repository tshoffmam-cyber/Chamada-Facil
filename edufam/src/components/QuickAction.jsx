export default function QuickAction({ icon, title, subtitle }) {
    return (
          <button className="quick-action">
                <div className="quick-icon">{icon}</div>div>
                <div>
                        <strong>{title}</strong>strong>
                        <span>{subtitle}</span>span>
                </div>div>
          </button>button>
        );
}</button>
